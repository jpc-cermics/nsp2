include ../../Makefile.incl

CFLAGS=`$(PKG_CONFIG) vte-2.90 --cflags --libs`

all	::  ../../bin/zterm

../../bin/zterm :  $(TERM_NAME).c
	$(CC) -o ../../bin/zterm vteapp. $(LDFLAGS)

distclean::
	$(RM) -f ../../bin/zterm
