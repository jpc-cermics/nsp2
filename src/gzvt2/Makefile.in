include ../../Makefile.incl

CFLAGS=`$(PKG_CONFIG) @VTE_PKG@ --cflags --libs`

all	::  ../../bin/zterm

../../bin/zterm :  @VTE_FILE@
	$(CC) -o ../../bin/zterm @VTE_FILE@ $(CFLAGS)

distclean::
	$(RM) -f ../../bin/zterm

clean::
