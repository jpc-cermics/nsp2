% -*- mode: latex -*-

\mansection{union}
\begin{mandesc}
  \short{union}{compute the union of two vectors or two lists}
\end{mandesc}

% -- Calling sequence section
\begin{calling_sequence}
\begin{verbatim}
C = union(A,B)
[C [,kA, kB]] = union(A,B)
\end{verbatim}
\end{calling_sequence}
% -- Parameters
\begin{parameters}
  \begin{varlist}
    \vname{A,B} : both vectors of numbers, strings or cells , or both lists.
    \vname{C} : vector or list with the union of elements of A and B.
    \vname{kA, kB} : vectors of indices such that $C = A(kA) \cup B(kB)$.
  \end{varlist}
\end{parameters}

\begin{mandescription}
  This function computes the set union $C = A \cup B$
  between 2 vectors (of numbers, strings or cells) or between 2
  lists considered as sets (but uniqueness of each element in A or B is not
  required). If A or B are matrices, they are considered as a big column vectors.
  On output, $C$, $kA$ and $kB$ are row vectors if  $A$ and $B$ are both row vectors, otherwise they
  are column vectors. For numbers or strings $C$ is sorted.
\end{mandescription}

\begin{examples}

\paragraph{example 1} with vectors of numbers:
\begin{program}\HCode{A = [0.5, -1, 2];\Hnewline
B = [2, 2, -2, 4.4];\Hnewline
C = union(A,B)\Hnewline
[C, kA, kB] = union(A,B)\Hnewline
// we must have C = sort([A(kA),B(kB),"g","i"],)\Hnewline
C.equal[sort([A(kA),B(kB)],"g","i")]}
\end{program}

\paragraph{example 2} with vectors of strings:
  \begin{program}\HCode{A = ["beer", "red vine"]\Hnewline
    B = [ "foo", "bar", "beer"]\Hnewline
    C = union(A,B)\Hnewline
    [C, kA, kB] = union(A,B)\Hnewline
// we must have C = sort([A(kA),B(kB),"g","i"],)\Hnewline
C.equal[sort([A(kA),B(kB)],"g","i")]}
  \end{program}

\paragraph{example 3} with vectors of cells:
  \begin{program}\HCode{A = {"toto", [0,1], 1, 2, 1, "bar"}\Hnewline
    B = { "foo",  3, [0,1], [2;3]}\Hnewline
    C = union(A,B)}
  \end{program}

\paragraph{example 4} with lists:
  \begin{program}\HCode{A = list("toto", [0,1], 1, 2, 1, "bar")\Hnewline
    B = list( "foo", 3, [0,1], [2;3] )\Hnewline
    C = union(A,B)}
  \end{program}

\end{examples}

\begin{manseealso}
  \manlink{unique}{unique} \manlink{setdiff}{setdiff}  
\end{manseealso}

% -- Authors
\begin{authors}
  Bruno Pincon
\end{authors}
